# 開発環境用のDockerfile
FROM node:20-alpine

WORKDIR /app
# 受け取った公開環境変数を dev サーバーに反映
ARG NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN
ENV NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=$NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN
ARG NEXT_PUBLIC_MLIT_API_KEY
ENV NEXT_PUBLIC_MLIT_API_KEY=$NEXT_PUBLIC_MLIT_API_KEY

# パッケージファイルをコピー
COPY package*.json ./

# 依存関係をインストール
RUN npm install

# アプリケーションコードをコピー
COPY . .

# ポートを公開
EXPOSE 3000

# 開発サーバーを起動
CMD ["npm", "run", "dev"]
